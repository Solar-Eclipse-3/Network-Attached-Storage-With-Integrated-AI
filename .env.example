# Backend secrets (DO NOT commit real keys)
# Copy this file to .env and fill values before running the backend.

# Example: OpenAI API key (DO NOT put a real key here)
OPENAI_API_KEY=
# For testing lower-cost models set this value, otherwise leave blank and set in your local .env
OPENAI_MODEL=gpt-3.5-turbo
# Any other backend secrets (keep empty in example)
API_KEY=
