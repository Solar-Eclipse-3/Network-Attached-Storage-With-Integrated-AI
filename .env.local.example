# Frontend env (client-side). Copy to .env.local to override in development.
# Note: VITE_ prefixed variables are exposed to client code.

VITE_API_BASE=/api
# Public (non-secret) API key example (only use for keys intended for client-side)
VITE_API_KEY=
