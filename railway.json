{
  "_comment": "WARNING: Railway deployment requires persistent volumes (not configured). This file is kept for reference only. Run locally instead: python chatbot_server.py",
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash build.sh"
  },
  "deploy": {
    "startCommand": "python chatbot_server.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}